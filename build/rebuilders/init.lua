return {
   dirty_bit = require "build.rebuilders.dirty_bit",
   verifying_traces = require "build.rebuilders.verifying-traces",
   mtime = require "build.rebuilders.mtime",
   phony_adapter = require "build.rebuilders.phony-adapter",
}
